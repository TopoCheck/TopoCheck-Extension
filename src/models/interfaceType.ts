/** Supported network interface categories. */
export enum InterfaceType {
  Ethernet = "ethernet",
  GigabitEthernet = "gigabit-ethernet",
  FastEthernet = "fast-ethernet",
  Loopback = "loopback",
  Vlan = "vlan"
}
